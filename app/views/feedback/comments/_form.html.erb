<h3>New comment</h3>
<%= form_with(model: [@ticket, @ticket.comments.build], local: true) do |form| %>
  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </p>
  <%= form.submit %>
<% end %>